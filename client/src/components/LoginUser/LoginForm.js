import React from 'react'

import LoginSubmitForm from '../../containers/LoginUser/LoginSubmitForm'

const LoginForm = (prop) => (
    <div>
        <LoginSubmitForm/>
    </div>
)

export default LoginForm;
